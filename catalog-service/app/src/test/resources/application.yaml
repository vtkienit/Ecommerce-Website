spring:
  datasource:
    url: jdbc:h2:mem:catalog-service;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DATABASE_TO_LOWER=TRUE;INIT=CREATE SCHEMA IF NOT EXISTS catalog
    driver-class-name: org.h2.Driver
    username: sa
    password: ""
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        default_schema: catalog
        hbm2ddl:
          create_namespaces: true

catalog:
  seed:
    enabled: false
